SELECT ANIMAL_ID, ANIMAL_TYPE, DATETIME,INTAKE_CONDITION,NAME,SEX_UPON_INTAKE  from ANIMAL_INS

// 테이블 안에 있는 데이터를 모두 보여주는거니 * 로 가능
// ANIMAL_ID 순으로 조회라서 ORDER BY 사용
SELECT *  from ANIMAL_INS ORDER BY ANIMAL_ID