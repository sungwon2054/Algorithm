// 그냥 COUNT만 사용하면 한개(Cat)만 나온다
// GROUP BY로 묶어 컬럼 값이 같은 것끼리 묶어준다
// ORDER BY로 출력 순서를 정해준다
SELECT ANIMAL_TYPE, COUNT(ANIMAL_TYPE)
FROM ANIMAL_INS 
GROUP BY ANIMAL_TYPE
ORDER BY 
CASE ANIMAL_TYPE
    WHEN 'Cat' THEN 1
    WHEN 'Dog' THEN 2
END

